# Outputs

This directory is intended for generated analysis results.

The files in this directory are produced by running the scripts in the `src/` directory and are therefore not included in the repository.
